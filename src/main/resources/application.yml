spring:
  profiles:
    active: dev   # 기본 active → 개발 환경
    group:
      dev:
        - secret
      prod:
        - secret

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  jackson:
    serialization:
      indent-output: true

# OAuth 설정
oauth:
  redirect-url: http://localhost:5173/oauth/redirect
  allowed-origins:
    - http://localhost:5173
    - http://127.0.0.1:5173

logging:
  level:
    root: INFO
    com.usinsa.backend.domain.auth.oauth: DEBUG  # OAuth 상세 로그
